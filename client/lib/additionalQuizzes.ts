import { QuizData } from "./quizzesData";

// This file previously generated additional quizzes but is no longer needed
// since we've reduced the total number of quizzes to 30
export const additionalQuizzes: QuizData[] = [];
